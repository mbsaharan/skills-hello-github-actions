name: Main Deployment Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  call-reusable-build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      environment_name: "production"
