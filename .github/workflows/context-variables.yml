name: CI

on: push

jobs:
  prod-check:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Output deployment branch
        run: echo "Deploying to production server on branch $GITHUB_REF"
